apiVersion: v1
kind: Service
metadata:
  name: static-nginx
  labels:
    app.kubernetes.io/name: static-nginx
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: static-nginx
  ports:
    - name: http
      port: 80
      targetPort: http


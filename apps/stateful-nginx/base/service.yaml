apiVersion: v1
kind: Service
metadata:
  name: stateful-nginx
  labels:
    app.kubernetes.io/name: stateful-nginx
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: stateful-nginx
  ports:
    - name: http
      port: 80
      targetPort: http

